# Task 5 report: load flowchart data from static DB export

## Status

Complete locally. The HTML runtime now bootstraps from the relative static artifact `data/derived/flowchart.json`; the browser does not open the database build products. The requested commit message is:

`feat: load flowchart data from static DB export`

The final commit contains the requested message; its hash is available from `git log` after this report is committed.

## Implemented

- Replaced the six HTML-owned fact literals (`NODES`, `EDGES`, `CHAR_LINKS`, `RELEASE_META`, `CHRONOLOGY_LANES`, and `CHRONOLOGY_META`) with empty runtime declarations.
- Added `validateFlowchartPayload`, `initializeFlowchartData`, and asynchronous `loadFlowchartData` bootstrap logic.
- The loader validates schema version `1`, node IDs and required node fields, edge endpoints and reason references, character work IDs, and the tracked `view_policy` coverage/shape.
- Runtime compatibility fields are reconstructed from DB work IDs, including adjacency maps, `reasonsById`, release metadata, and character work sets. Details and chronology metadata come from `view_policy`.
- Initial rendering, SVG interaction initialization, filter option population, and presentation refresh occur only after successful bootstrap. Japanese loading and failure status are exposed through an ARIA live status element.
- Preserved the existing relative URL behavior and controls, including the mobile chart path. No SQLite/WASM runtime dependency was added.
- Documented the static artifact/loading boundary in `README.md`.
- Added `tests/library_v5/test_index_db_export_contract.py` for the source-level bootstrap contract.

## Verification

All commands ran from the worktree root with the bundled Python runtime:

```powershell
$MarvelPython = 'C:\Users\ataka\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
& $MarvelPython -m unittest tests.library_v5.test_index_db_export_contract -v
```

Result: `Ran 4 tests ... OK`.

The focused contract suite initially supplied the RED checks (three intermediate source assertions failed while the loader/view-policy markers and initialization gate were being tightened); the final implementation is GREEN.

```powershell
& $MarvelPython -m unittest discover -s tests/library_v5 -p 'test_*.py' -v
```

Result: `Ran 221 tests in 18.683s — OK`.

```powershell
& $MarvelPython -m scripts.library_v5.build --repo-root .
```

Result: `audit_issue_count: 0`, `audit_ok: true`, `prewatch_edges: 199`, `story_paths_reproduced: 83`, `work_edges_all: 361`, `work_pair_reasons: 569`, and flowchart export counts `nodes: 131`, `edges: 361`, `reasons: 569`, `characters: 42`.

The same build command completed successfully while `index.html` was temporarily absent and restored afterward, confirming index-independent build/export behavior.

## Static HTTP smoke

A bounded local `http.server` smoke served the worktree and returned:

```text
{"html":200,"json":200,"schema":"1","nodes":131,"edges":361,"reasons":569,"characters":42}
```

The server was stopped immediately after the check. Generated DB/audit/flowchart outputs were inspected and removed; canonical `data/library/` inputs and `data/content_audit/reviews.csv` were not modified.

## Browser smoke

Using the local static server and the in-app browser:

- Desktop load reached `データを読み込みました（131作品）。`, state `ready`, and `131 / 131 作品`; branch options were populated (15 branches plus the placeholder), character options were populated (42 characters plus the placeholder), and console errors were empty.
- Selecting a work still rendered the existing detail panel. Switching to the public-order and chronology views generated one SVG for each view, with no console errors.
- At a temporary 390×844 viewport, bootstrap reached `ready`, the count remained `131 / 131 作品`, and the mobile canvas path created two canvases. The temporary viewport was reset afterward.

## Concerns and boundaries

- `flowchart.json` remains a generated deployment artifact, as established by the preceding export tasks; it is regenerated by the build before static packaging and was intentionally cleaned from this worktree after verification.
- Browser loading requires an HTTP(S) static host; opening the HTML directly as `file://` is not the supported deployment path.
- The required fresh `git fetch origin` check was attempted but the shared worktree Git metadata returned `Permission denied` for the fetch state file. No reset, overwrite, rebase, or remote mutation was performed; the starting HEAD remained `53e259d430df0f7556cb359af8f710d331a7be8d`.
- This report records local verification only; remote CI, review, merge, and publication remain outside this task.
