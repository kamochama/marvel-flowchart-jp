diff --git a/AUDIT.json b/AUDIT.json
index 1a03baf..3e49c3e 100644
--- a/AUDIT.json
+++ b/AUDIT.json
@@ -1,63 +1,61 @@
 {
-  "version": "5.14.0-public",
-  "baseVersion": "5.13.0-public",
+  "version": "5.14.1-public",
+  "baseVersion": "5.14.0-public",
   "date": "2026-08-23",
   "data": {
     "works": 131,
     "directedEdges": 199,
     "characterLinks": 155
   },
-  "mobileNavigation": {
-    "persistentSingleSearchInput": true,
-    "horizontalTabStripHidden": true,
-    "areaMenuTargets": ["overview", "mcu", "legacy", "road", "doomsday", "characters"],
-    "watchExcludedFromAreaMenu": true,
-    "watchWorkspaceAction": true,
-    "watchWorkspaceLabelSync": true,
-    "areaSheetWithin390x844Viewport": true,
-    "oldGuideHiddenOnMobile": true,
-    "mobileSearchPlaceholder": "作品を検索…"
+  "featuredRouteFix": {
+    "previewExclusiveFromNormalRendering": true,
+    "clearExitsPreview": true,
+    "scopeChangeExitsPreview": true,
+    "importanceChangeExitsPreview": true,
+    "searchAndFilterExitPreview": true,
+    "combineAndPrepControlsExitPreview": true,
+    "existingGoalsPreserved": true,
+    "existingGoalLightingRestored": true,
+    "featuredTargetClearedOnExit": true,
+    "dynamicHighlightEdgesClearedOnExit": true,
+    "queuedFrameRaceGuard": true,
+    "immediateExitRestoresExistingGoal": true
   },
-  "desktopNavigation": {
-    "sevenTabsPreserved": true,
-    "featuredEntryCompact": true
-  },
-  "searchNavigation": {
-    "doesNotChangeGoals": true,
-    "singleQueryState": true,
-    "panelPriority": ["overview", "mcu", "road", "legacy", "doomsday"],
-    "centersResultNode": true
-  },
-  "featuredRoute": {
-    "enabled": true,
-    "targetId": "avengers-doomsday-2026-12-18",
+  "desktopFeaturedEntry": {
+    "separatedFromSearchControl": true,
     "label": "ドゥームズデイへの道",
-    "previewDoesNotChangeGoals": true,
-    "incomingStrongClosure": true,
-    "directContextOneHop": true,
-    "outgoingTraversal": false,
-    "secretWarsAutoIncluded": false,
-    "existingGoalsRestoredOnExit": true,
-    "normalGoalLogicForAdd": true,
-    "configurationSingleSource": true
+    "fullLabelVisibleAt1280": true,
+    "fullLabelVisibleAt1440": true,
+    "mobileCardStructurePreserved": true
   },
-  "regressionAgainstV5130": {
-    "or": {"pairs": 8515, "hashFnv1a64": "c7b1741074835354", "exact": true},
-    "and": {"pairs": 8515, "hashFnv1a64": "0b2c0975e1dd7845", "exact": true},
-    "path": {"pairs": 8515, "hashFnv1a64": "f614d9a7bcafbfb2", "exact": true},
-    "singleGoalPlans": {"count": 393, "hashFnv1a64": "4d3822b4e5fba9f1", "exact": true}
+  "regressionAgainstV5140": {
+    "or": {
+      "pairs": 8515,
+      "hashFnv1a64": "9c38afad0f8ac3fe",
+      "exact": true
+    },
+    "and": {
+      "pairs": 8515,
+      "hashFnv1a64": "ad48d8c46ae1bd61",
+      "exact": true
+    },
+    "path": {
+      "pairs": 8515,
+      "hashFnv1a64": "8b9847fcda5cdf96",
+      "exact": true
+    },
+    "singleGoalPlans": {
+      "count": 393,
+      "hashFnv1a64": "a3c6f1c12199a903",
+      "exact": true
+    }
   },
-  "interactionRegression": {
-    "desktopPlainWheelZoom": true,
-    "wheelOutsideChartUntouched": true,
-    "mobileOneFingerPan": true,
-    "mobileTwoFingerPinch": true,
-    "lastGoalRemovalFullRelightDesktop": true,
-    "lastGoalRemovalFullRelightMobile": true,
-    "dynamicHighlightEdgesCleared": true,
+  "interactionSmoke": {
+    "featuredGoalAddUsesNormalSelection": true,
+    "lastGoalRemovalFullRelight": true,
+    "mobileCanvasOverviewActive": true,
+    "mobileSearchRetiresFeaturedPreview": true,
     "sharedRoomDialog": true,
-    "watchedCheckbox": true,
-    "goalRemoval": true,
     "pageScriptErrors": 0
   },
   "worker": {
@@ -66,7 +64,14 @@
     "frontendChangeRequiresRedeploy": false
   },
   "package": {
-    "rootFiles": ["index.html", "README.md", "AUDIT.md", "AUDIT.json", "preview.png", ".nojekyll"],
+    "rootFiles": [
+      "index.html",
+      "README.md",
+      "AUDIT.md",
+      "AUDIT.json",
+      "preview.png",
+      ".nojekyll"
+    ],
     "exactSixFiles": true
   },
   "allPass": true
diff --git a/AUDIT.md b/AUDIT.md
index 4d8523d..055fbae 100644
--- a/AUDIT.md
+++ b/AUDIT.md
@@ -1,69 +1,57 @@
-# マーベル作品相関図 日本版 — PUBLIC v5.14.0 監査
+# マーベル作品相関図 日本版 — PUBLIC v5.14.1 監査
 
-監査日: 2026-08-23
-基準版: PUBLIC v5.13.0
-候補版: PUBLIC v5.14.0
+監査日: 2026-08-23  
+基準版: PUBLIC v5.14.0  
+候補版: PUBLIC v5.14.1
 
 ## 結果
 
-**全体回帰: PASS**
-**スマホ階層ナビ: PASS**
-**常設検索（ゴール非変更）: PASS**
-**「今見るなら」特集ルート: PASS**
-**PC 7タブ維持: PASS**
-**既存操作・予習・共有UI: PASS**
+**全体回帰: PASS**  
+**特集プレビュー終了処理: PASS**  
+**既存ゴール点灯の復元: PASS**  
+**PC「ドゥームズデイへの道」全文表示: PASS**  
+**PC / スマホ既存UI: PASS**
 
-## v5.14.0 ナビゲーション
+## v5.14.1 修正
 
-- スマホでは従来の横並び7タブを非表示にし、上部に常設検索と現在エリアのセレクタを配置。
-- スマホの表示エリアは「基本 / 拡張 / 調べる」に整理し、主要フローを既定表示とした。
-- モバイルメニューは `主要フロー / MCU本流 / マルチバース / ストリート／旧TV / イベント合流 / 人物・組織` の6項目。`この作品を見るなら` は図メニューから外した。
-- ゴール選択後の `予習プランを見る` から下部の予習・視聴プランへ移動し、表示中はモバイルナビも `予習プラン` と表示する。
-- PCでは従来の7タブを維持。
-- スマホの旧「はじめかた」横スクロール列は、常設ナビと役割が重複するため非表示。
-- bottom sheet は `body` 直下へ移し、sticky header の `backdrop-filter` による fixed 座標ずれを回避。390×844で全体がビューポート内に収まることを確認。
+- `流れを見る` は引き続きゴール集合を変更しない一時プレビュー。
+- 特集プレビュー中に `全解除 / 点灯範囲 / 接続層 / OR・AND・PATH / 予習プラン詳しさ / 検索・絞り込み` など通常描画へ戻る操作を開始した場合、先に特集状態を終了してから通常処理を実行する。
+- 通常の `render()` / `refreshSelection()` 経路にも特集終了ガードを追加し、特集状態だけ残る不整合を防止。
+- 検索欄など、イベント登録時に旧 `render` 関数を直接保持している既存イベントについては capture 段階で特集終了を行う。
+- 特集開始前にゴールが存在した場合、通常操作へ戻る際に `selectedIds` を保持して元のゴール点灯を復元する。
+- 特集終了時に `featured-target` と動的ハイライトを除去し、通常描画へ戻す。
+- 特集開始直後に別操作へ移った場合でも、予約済みの古い描画フレームが復元済みゴール点灯を再消灯しないようガード。
+- PCでは特集入口を検索欄内部から独立行へ移動し、`ドゥームズデイへの道` の文字を縮ませない。1280px / 1440pxで全文表示を確認。
+- スマホの特集カード構成は維持。
 
-## 常設検索
-
-- PC/スマホとも同じ `#q` input 1個を使用し、検索状態を複製しない。
-- スマホでは `作品を検索…` と短い表示にし、sticky header 内に常設。
-- 検索候補選択はゴール集合 `selectedIds` を変更しない。
-- 現在の図に作品があれば維持し、無ければ `overview → mcu → road → legacy → doomsday` の優先順で表示可能な図へ移動。
-- 検索移動後は対象ノードへ移動・一時フォーカスし、ユーザーがノードをタップした場合のみ通常のゴール追加になる。
-
-## 「今見るなら」特集ルート
-
-- 初期特集: `ドゥームズデイへの道`。
-- 対象ID: `avengers-doomsday-2026-12-18`。
-- `流れを見る` は `selectedIds / selected` を変更しない一時プレビュー。
-- strong / very strong の incoming 接続を最後まで遡り、許可された直接の medium / weak 文脈接続だけ1段追加。
-- outgoing は辿らず、`Avengers: Secret Wars` 側へ自動延長しないことを確認。
-- プレビュー終了時、開始前のゴールが存在すれば通常のゴール点灯を復元。
-- `ゴールに追加` は既存ゴールロジックへ接続。
-- 特集設定は `FEATURED_ROUTE` 1か所に集約し、将来は対象ID・表示名・説明を差し替え可能。
-
-## データ・ロジック回帰（v5.13.0 比較）
+## データ・ロジック回帰（v5.14.0 比較）
 
 - 作品: 131
 - 有向接続: 199
 - 人物リンク: 155
-- OR: 8,515組 — 完全一致（FNV-1a 64: `c7b1741074835354`）
-- AND: 8,515組 — 完全一致（FNV-1a 64: `0b2c0975e1dd7845`）
-- PATH: 8,515組 — 完全一致（FNV-1a 64: `f614d9a7bcafbfb2`）
-- 単一ゴール予習: 393パターン — 完全一致（FNV-1a 64: `4d3822b4e5fba9f1`）
-
-## 操作回帰
-
-- PCチャート上ホイール直接ズーム: PASS
-- PCチャート外ホイール非捕捉: PASS
-- スマホ1本指パン: PASS
-- スマホ2本指ズーム: PASS
-- 最後のゴール解除後の全点灯（PC / スマホCanvas）: PASS
-- 動的ハイライト矢印の解除: PASS
+- OR: 8,515組 — 完全一致（FNV-1a 64: `9c38afad0f8ac3fe`）
+- AND: 8,515組 — 完全一致（FNV-1a 64: `ad48d8c46ae1bd61`）
+- PATH: 8,515組 — 完全一致（FNV-1a 64: `8b9847fcda5cdf96`）
+- 単一ゴール予習: 393パターン — 完全一致（FNV-1a 64: `a3c6f1c12199a903`）
+
+## 修正再現テスト
+
+- `流れを見る → 全解除`: 特集状態OFF / `featured-target` 0 / 全点灯復帰 — PASS
+- `流れを見る → 点灯範囲変更`: 特集状態OFF — PASS
+- `流れを見る → 接続層変更`: 特集状態OFF — PASS
+- 既存ゴールあり → `流れを見る → 検索入力`: 特集状態OFF、ゴール集合不変、元の点灯ノード・矢印数へ復帰 — PASS
+- 同操作を描画フレーム待ちなしで5回連続実行し、元の点灯ノード45 / 矢印56へ毎回復帰 — PASS
+- PC 1280px: `ドゥームズデイへの道` 省略なし — PASS
+- PC 1440px: `ドゥームズデイへの道` 省略なし — PASS
+
+## 操作スモーク
+
+- 特集 `ゴールに追加` → 既存ゴールロジック: PASS
+- 最後のゴール全解除 → 全点灯: PASS
+- スマホCanvas overview: PASS
+- スマホ常設検索から特集終了: PASS
 - Shared Room ダイアログ: PASS
-- 視聴済みチェック: PASS
-- ゴール個別解除: PASS
-- ブラウザスクリプトエラー: 0
+- PC / スマホ page script errors: 0
 
 ## 配布条件
 
@@ -76,4 +64,4 @@ GitHub Pages用ZIPはルート直下6ファイル固定:
 - `preview.png`
 - `.nojekyll`
 
-READMEは一般利用者向けの短い説明のみとし、内部実装や大量の監査情報は `AUDIT.md / AUDIT.json` に分離する。
+READMEは一般利用者向けの短い説明のみとし、内部実装・監査数値は `AUDIT.md / AUDIT.json` に分離する。
diff --git a/index.html b/index.html
index ae1252b..55e3195 100644
--- a/index.html
+++ b/index.html
@@ -598,12 +598,17 @@ main{grid-template-columns:minmax(0,1fr) clamp(310px,25vw,360px)}
 
 
 /* PUBLIC v5.14.0 — configurable featured route entry. */
-.desktop-featured-host{display:inline-flex;min-width:0}
+.controls-featured-row{display:flex;max-width:1920px;margin:7px auto 0;min-width:0}
+.desktop-featured-host{display:inline-flex;min-width:420px;max-width:100%;padding:6px 8px;border:1px solid #38263b;border-radius:11px;background:linear-gradient(180deg,rgba(39,18,34,.88),rgba(18,22,36,.9))}
+.desktop-featured-host:empty{display:none}
 .mobile-featured-host{display:none}
 .featured-route-control{display:flex;align-items:center;gap:6px;min-width:0}
+.desktop-featured-host .featured-route-control{width:100%}
 .featured-route-copy{display:flex;align-items:center;gap:5px;min-width:0;white-space:nowrap}
+.desktop-featured-host .featured-route-copy{flex:1 1 auto}
 .featured-route-copy .featured-eyebrow{font-size:9px;font-weight:900;color:#fda4af}
 .featured-route-copy strong{font-size:11px;overflow:hidden;text-overflow:ellipsis}
+.desktop-featured-host .featured-route-copy .featured-eyebrow,.desktop-featured-host .featured-route-copy strong{flex:0 0 auto;overflow:visible;text-overflow:clip}
 .featured-route-copy small{display:none;color:#94a3b8;font-size:9px}
 .featured-route-control button{min-height:32px;padding:5px 8px;font-size:10px;white-space:nowrap}
 .featured-route-preview{border-color:#be123c!important;background:#3a101d!important;color:#fecdd3!important}
@@ -618,6 +623,7 @@ main{grid-template-columns:minmax(0,1fr) clamp(310px,25vw,360px)}
   .featured-route-copy{display:block;min-width:0}.featured-route-copy .featured-eyebrow{display:block;margin-bottom:1px}.featured-route-copy strong{display:block;font-size:11px}.featured-route-copy small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
   .featured-route-control button{min-height:38px;padding:6px 7px;font-size:10px}
   .desktop-featured-host{display:none}
+  .controls-featured-row{display:none}
 }
 
 </style>
@@ -632,7 +638,7 @@ main{grid-template-columns:minmax(0,1fr) clamp(310px,25vw,360px)}
     </div>
     <div class="brand-meta" aria-label="サイト情報">
       <span class="stat-chip"><b>131</b> 作品</span><span class="stat-chip"><b>199</b> 有向接続</span>
-      <span class="stat-chip public-release">PUBLIC v5.14.0</span>
+      <span class="stat-chip public-release">PUBLIC v5.14.1</span>
       <button id="publicHelpBtn" class="public-help-btn" type="button">? 使い方</button>
     </div>
   </div>
@@ -651,11 +657,12 @@ main{grid-template-columns:minmax(0,1fr) clamp(310px,25vw,360px)}
   </div>
   <div class="controls" aria-label="作品相関図の操作">
   <div class="controls-primary">
-    <div class="control-group control-search" aria-label="作品を探す"><input id="q" aria-label="作品を検索" placeholder="邦題・英題・シリーズ名で検索"><select id="branch" aria-label="系統で絞り込み"><option value="">すべての系統</option></select><span id="desktopFeaturedHost" class="desktop-featured-host"></span></div>
+    <div class="control-group control-search" aria-label="作品を探す"><input id="q" aria-label="作品を検索" placeholder="邦題・英題・シリーズ名で検索"><select id="branch" aria-label="系統で絞り込み"><option value="">すべての系統</option></select></div>
     <div class="control-group control-scope"><span class="muted">点灯範囲</span><button class="scope-btn active" data-scope="all" title="strong / very strong の物語方向を前後に辿り、medium は直接隣接だけ表示">関連全体</button><button class="scope-btn" data-scope="one">1段階</button><button class="scope-btn" data-scope="two">2段階</button><span class="direction-key" title="関連全体では途中で向きを切り替えません"><i class="bk"></i>←前史 <i class="fw"></i>→後続 <i class="cx"></i>直接補助</span></div>
     <div class="control-group control-importance"><span class="muted">接続層</span><span class="importance-key"><button class="importance-btn" data-importance-mode="core" title="中核接続だけ表示">中核</button><button class="importance-btn" data-importance-mode="recommended" title="中核＋推奨を表示">＋推奨</button><button class="importance-btn active" data-importance-mode="reference" title="中核＋推奨＋参照の全199接続を表示（既定）">全線</button></span></div>
     <div class="control-group control-reset"><button id="clear">全解除</button></div>
   </div>
+  <div class="controls-featured-row" aria-label="今見るなら"><span id="desktopFeaturedHost" class="desktop-featured-host"></span></div>
   <details class="advanced-controls">
     <summary>詳細設定 <span class="muted">人物・複数ゴール・PATH・系列・ズーム</span></summary>
     <div class="advanced-grid">
@@ -7489,7 +7496,7 @@ render();
     const state=selectedIds.size ? (window.__marvelLastSelectionState||computeSelectionState()) : null;
     const svg=document.querySelector('.panel.active .svg-wrap svg');
     return {
-      version:'5.14.0-public', selected:[...selectedIds], scope:scopeMode, importance:importanceMode, combine:combineMode,
+      version:'5.14.1-public', selected:[...selectedIds], scope:scopeMode, importance:importanceMode, combine:combineMode,
       dim:!!svg?.classList.contains('dim'),
       litNodes:svg?.querySelectorAll('g.node.hl,g.node.focus').length||0,
       litEdges:svg?.querySelectorAll('g.edge.hl').length||0,
@@ -7827,7 +7834,7 @@ render();
 
 
 
-<footer class="public-footer"><div class="footer-row"><div><strong>マーベル作品相関図 日本版</strong> — 自分用に作ったものの公開版です。<strong>継続的な保守・更新・サポートは予定していません。</strong> 非公式・非商用。</div><span class="footer-version">PUBLIC v5.14.0 · 2026-08-23</span></div><div>企画・実装・データ整理・監査の多くに生成AIを利用しています。人手でも確認していますが、情報が古い・誤り・不具合が残る場合があります。Issue / Pull Request への対応も保証しません。Marvel / Disney および各作品・キャラクターに関する商標・著作権は各権利者に帰属し、本サイトは権利者各社とは提携していません。</div></footer>
+<footer class="public-footer"><div class="footer-row"><div><strong>マーベル作品相関図 日本版</strong> — 自分用に作ったものの公開版です。<strong>継続的な保守・更新・サポートは予定していません。</strong> 非公式・非商用。</div><span class="footer-version">PUBLIC v5.14.1 · 2026-08-23</span></div><div>企画・実装・データ整理・監査の多くに生成AIを利用しています。人手でも確認していますが、情報が古い・誤り・不具合が残る場合があります。Issue / Pull Request への対応も保証しません。Marvel / Disney および各作品・キャラクターに関する商標・著作権は各権利者に帰属し、本サイトは権利者各社とは提携していません。</div></footer>
 
 <script>
 (()=>{
@@ -8080,7 +8087,17 @@ window.FEATURED_ROUTE=Object.freeze({
     if(!cfg.enabled||!nm[cfg.targetId])return false;
     featuredRouteState={targetId:cfg.targetId,state:buildFeaturedRouteState(cfg.targetId)};
     activatePanel('overview',{fit:false,restoreSelection:false,exitFeatured:false});
-    requestAnimationFrame(()=>{renderFeaturedRoutePreview();_origCenterNodeInView(cfg.targetId);});
+    requestAnimationFrame(()=>{
+      // v5.14.1: activatePanel also queues a paint. If the user exits the
+      // preview before this frame, that older paint must not erase the
+      // selection we just restored.
+      if(!featuredRouteState){
+        if(selectedIds.size)refreshSelection(false);
+        else{clearSvg();render();}
+        return;
+      }
+      if(renderFeaturedRoutePreview())_origCenterNodeInView(cfg.targetId);
+    });
     return true;
   };
   window.exitFeaturedRoutePreview=function({restore=true}={}){
@@ -8103,6 +8120,33 @@ window.FEATURED_ROUTE=Object.freeze({
     forward:[...(featuredRouteState?.state?.forward||[])]
   });
 
+  // v5.14.1: featured preview is exclusive. Any normal render path first
+  // retires the temporary preview state, then performs the requested repaint.
+  const renderBeforeFeaturedRoute=render;
+  render=function(...args){
+    if(featuredRouteState)exitFeaturedRoutePreview({restore:true});
+    return renderBeforeFeaturedRoute(...args);
+  };
+  const refreshSelectionBeforeFeaturedRoute=refreshSelection;
+  refreshSelection=function(...args){
+    if(featuredRouteState)exitFeaturedRoutePreview({restore:false});
+    return refreshSelectionBeforeFeaturedRoute(...args);
+  };
+
+  const retireFeaturedBeforeNormalAction=()=>{
+    if(featuredRouteState)exitFeaturedRoutePreview({restore:true});
+  };
+  const featuredExitFieldSelector='#q,#branch,#status,#character,#familyFocus';
+  document.addEventListener('input',e=>{
+    if(e.target?.matches?.(featuredExitFieldSelector))retireFeaturedBeforeNormalAction();
+  },true);
+  document.addEventListener('change',e=>{
+    if(e.target?.matches?.(featuredExitFieldSelector))retireFeaturedBeforeNormalAction();
+  },true);
+  document.addEventListener('pointerdown',e=>{
+    if(e.target?.closest?.('#clear,.scope-btn,.importance-btn,.combine-btn,.path-pref-btn,.prep-tier'))retireFeaturedBeforeNormalAction();
+  },true);
+
   const selectBeforeFeaturedRoute=select;
   select=function(id,multi=false){
     if(featuredRouteState)exitFeaturedRoutePreview({restore:false});
